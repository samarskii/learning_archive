class Solution(object):
    def addDigits(self, num):
        """
        :type num: int
        :rtype: int
        """
        while len(num) > 1:
            num = str(num)
            num = [i for i in num]
            num = sum(num)
            
            number = 0
            for i in num:
                number 
        
            if len(num) == 1:
                return num
            
alef = 13
alef = str(alef)
alef = [i for i in alef]
alef = sum(alef)

print(alef)            